name: github-pages deploy
on:
  push:
    paths: ["/**"] # Trigger the action only when files change in the folders defined here
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install and Build 🔧
        run: |
          npm i
          npm run storybook-deploy-gh-pages
